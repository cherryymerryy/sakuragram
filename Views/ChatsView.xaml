<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CherryMerryGram.Views.ChatsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CherryMerryGram.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Horizontal" Padding="0, 50, 0, 0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <ScrollView>
            <StackPanel x:Name="ChatsList" Width="500px" />
        </ScrollView>
        <ScrollView>
            <StackPanel x:Name="Chat" />
        </ScrollView>
    </StackPanel>
</Page>
