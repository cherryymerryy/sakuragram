<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CherryMerryGramDesktop.Views.Chats.Chat"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    HorizontalContentAlignment="Stretch"
    VerticalContentAlignment="Stretch">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <Grid VerticalAlignment="Top" HorizontalAlignment="Stretch" Height="50" MaxHeight="50" MinHeight="50" Row="0" Padding="5">
            <Border x:Name="TopBar" Background="#1a1a1a" HorizontalAlignment="Stretch" CornerRadius="4" Padding="5">
                <StackPanel x:Name="ChatInfo" Orientation="Horizontal"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch">
                    <Button x:Name="Back" Content="Back" Click="Back_OnClick" HorizontalAlignment="Left" />
                    <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Stretch">
                        <TextBlock x:Name="ChatTitle" FontSize="18" FontStyle="Oblique" TextAlignment="Center" />
                        <TextBlock x:Name="ChatMembers" FontSize="14" TextAlignment="Center"/>
                    </StackPanel>
                    <Button x:Name="Call" Content="Call" Click="Call_OnClick" HorizontalAlignment="Right" IsEnabled="False" />
                </StackPanel>
            </Border>
        </Grid>
        
        <Grid VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Row="1">
            <Border>
                <Border.Background>
                    <ImageBrush x:Name="ThemeBackground" />
                </Border.Background>
                <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <StackPanel x:Name="MessagesList"/>
                </ScrollViewer>
            </Border>
        </Grid>
        
        <Grid HorizontalAlignment="Stretch" Height="50" MaxHeight="50" MinHeight="50" Row="2" Padding="5">
            <Border Background="#1a1a1a" CornerRadius="4" Padding="5">
                <StackPanel x:Name="UserActionsPanel" Orientation="Horizontal">
                    <TextBox x:Name="UserMessageInput" PlaceholderText="Enter your message" HorizontalAlignment="Stretch" />
                    <Button x:Name="Stickers" Content="Stickers" MaxHeight="25" MaxWidth="25" 
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch" />
                    <Button x:Name="SendMessage" Content="Send" MaxHeight="25" MaxWidth="25" Click="SendMessage_OnClick"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>